[project] 
name = "RDSDriver"
version = "1.4.2.dev" 
dependencies = [
    "pymysql",
    "psycopg2",
    # "psycopg2-binary"
]

[build-system] 
requires = ["hatchling"] 
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
only-include = ["src"]
sources = ["src"]

[tool.hatch.build.hooks.custom]