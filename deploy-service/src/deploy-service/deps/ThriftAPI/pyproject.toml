[project] 
name = "apis"
version = "0.0.1" 
dependencies = [
    "thrift"
]

[build-system] 
requires = ["hatchling"] 
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
only-include = ["src"]
sources = ["src"]

[tool.hatch.build.hooks.custom]