{{- if not $.Values.storage.storageClassName }}
{{- $r := int $.Values.replicaCount }}
{{- range $i, $v := list "0" "1" "2" }}
{{- if lt $i $r }}
{{- range $key, $value := $.Values.storage.local }}
{{- if eq $v $key }}
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: policy-engine-datadir-{{ template "policy-engine.name" $ }}-{{ $i }}
  namespace: {{ $.Values.namespace }}
spec:
  storageClassName: ""
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ $.Values.storage.capacity }}
  selector:
    matchLabels:
      app: {{ template "policy-engine.name" $ }}
      podindex: '{{ $i }}'
{{- end }}
{{- end }}
{{- end }}
{{- end }}
{{- end }}
