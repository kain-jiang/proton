apiVersion: v1
kind: Secret
metadata:
  name: webhook-server-cert
  namespace: proton-rds-mariadb-operator-system
type: kubernetes.io/tls
data: {}
  # TLS certificate and key must be provided at deploy time or generated dynamically.
  # Do NOT commit real certificates or private keys to the repository.
