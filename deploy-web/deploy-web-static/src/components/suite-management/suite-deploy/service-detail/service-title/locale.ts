import { i18nDeploy as i18n } from "../../../../../core/mediator";

export default i18n([
    ["版本：${version}", "版本：${version}", "Version: ${version}"],
    [
        "更新时间：${updateTime}",
        "更新時間：${updateTime}",
        "Update time: ${updateTime}",
    ],
    ["套件标识：${name}", "套件標識：${name}", "Suite identifier: ${name}"],
]);
