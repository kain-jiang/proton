package mariadb

const (
	ClusterNamespace = "resource"

	// 访问 MariaDB 所用的端口
	ClusterMariaDBServicePort = 3330

	ClusterServiceName = "mariadb-mariadb-cluster"

	MasterServiceName = "mariadb-mariadb-master"
)
